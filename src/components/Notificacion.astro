---
const { color , message } = Astro.props;
---

<div id="notificacion" class={`fixed bottom-5 right-5 ${color} text-white px-4 py-2 rounded-lg shadow-lg opacity-0 pointer-events-none transition-opacity duration-300 flex items-center gap-2`}>
    <span id="notificacionMensaje">{message}</span>
</div>