---
const { color , icon , link, clasesExtra } = Astro.props;
---

<a href={link} class={`${color} ${clasesExtra} w-12 h-12 flex items-center justify-center rounded-full text-white transition shadow`}><i class={`${icon} text-lg`}></i></a>
